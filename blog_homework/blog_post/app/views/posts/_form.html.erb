<% if @comments.errors.any? %>

  <h3><%= pluralize(@comments.errors.count, "error") %> prohibited this comment from being saved:</h3>


  <ul>
    <% @comment.errors.full_messages.each do |msg| %>
    <li> <%= msg %> </li>
    <% end %>
  </ul>

<% end %>








<%= form_for([@post])do |f| %>
	
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :body %>
	<%= f.number_field :body %>
	

	<%= f.submit %>

<% end %>